# Tabulator-Jest Experimental Project

To setup locally, clone this repo using `git clone https://github.com/famcodings/tabulator-jest-test.git` and run the following commands,
``` 
$ npm i
$ npm run dev
$ npm run start
```